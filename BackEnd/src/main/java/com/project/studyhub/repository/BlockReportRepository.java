package com.project.studyhub.repository;

import com.project.studyhub.entity.BlockReport;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BlockReportRepository extends JpaRepository<BlockReport, Long> {
}